# 巴哈貼文轉換規則

整理自使用者的 `bahamut-post` 技能及本次實測，查證日期：2026-09-07。技能是作者的排版約定，不代表巴哈官方完整語法規格。

| 輸入               | 巴哈輸出                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------- |
| 標題               | `[div][b]標題[/b][/div]`，不自動加編號或分隔線                                           |
| 段落及換行         | 每一視覺行使用獨立 `[div]`；跨行格式重新配對                                             |
| 粗體、斜體、刪除線 | `[b]`、`[i]`、`[s]`，置於區塊內                                                          |
| 分隔線             | 自閉合 `[hr]`                                                                            |
| 清單               | `[ul]`／`[ol]` 包 `[li]`，保留巢狀                                                       |
| 非 1 起始有序清單  | 文字編號與逐項區塊，避免起始值被重設                                                     |
| 工作清單           | 清單項目開頭的 `[ ]`、`[x]` 改為 ☐、☑                                                    |
| 表格               | `[table width=100% border=1 cellspacing=0 cellpadding=4]`，內含 `[tr]`、`[td]`；表頭加粗 |
| 引用               | 「引用：」及段落，不使用 `[quote]`                                                       |
| 連結、圖片         | HTTP(S) 的 `[url=…]文字[/url]`、自閉合 `[img=…]`                                         |
| 不可用網址         | 移除連結功能，保留標籤或圖片說明，顯示提醒                                               |
| 行內程式碼         | 原文文字，移除反引號                                                                     |
| 程式碼區塊         | 每行 `[div]`，保留空白行，Tab 預設四欄定位展開；空格使用 `&#160;`                        |
| 原始 HTML          | 跳脫為文字，不執行                                                                       |
| 公式               | 保留原文，不渲染；有辨識到公式時顯示提醒                                                 |

文字中的 `&`、`<`、`>`、引號依輸出格式編碼；巴哈原始碼另將字面中括號編碼為 `&#91;`、`&#93;`。程式碼內容不再做 Markdown 轉換。

URL 先解析再限制為 HTTP(S)，拒絕控制字元、空白與內嵌帳密。URL 中的中括號、引號等可能結束屬性的字元改為百分比編碼；HTML 屬性另作 HTML escaping。

## 與貼文技能的刻意差異

這是忠實轉換器，不是文章編輯助手，因此不自動重寫摘要、補來源、新增章節編號、重排表格或把敘述轉成表格。Markdown 既有連結、圖片與字體樣式視為明確的格式意圖並保留。

技能示例為了閱讀將各標記放在不同實體行；本次巴哈編輯器實測卻會將這些分隔換行轉成額外 `<br>`。本工具移除自產標記間的分隔換行，保留 `[div]` 代表的每一行與空白行，避免程式碼出現雙倍行距。原始碼不包含外層 Markdown 圍欄。

## 尚未支援

語法上色、AI 改寫、反向轉換、圖片上傳、公式排版、完整 GFM 擴充、相對網址解析與網站匯入。不擅自推測無法確認的語法支援。
