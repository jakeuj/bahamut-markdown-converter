# 巴哈貼文轉換規則

整理自使用者的 `bahamut-post` 技能及本次實測，查證日期：2026-09-08。技能是作者的排版約定，不代表巴哈官方完整語法規格。

| 輸入               | 巴哈輸出                                                                                                 |
| ------------------ | -------------------------------------------------------------------------------------------------------- |
| 標題               | 一至三級使用 `[h2]`／`[h3]`／`[h4]`；四至六級使用 `[div][b]標題[/b][/div]` 並提醒外觀合併；HTML 保留 h1–h6，不自動加編號或分隔線                                                           |
| 段落及換行         | 每一視覺行使用獨立 `[div]`；段落間一個空白區塊，跨行格式重新配對                                         |
| 粗體、斜體、刪除線 | `[b]`、`[i]`、`[s]`，置於區塊內                                                                          |
| 分隔線             | 自閉合 `[hr]`                                                                                            |
| 清單               | `[ul]`／`[ol]` 包 `[li]`，保留巢狀                                                                       |
| 非 1 起始有序清單  | 文字編號與手動縮排；已測 start/value 寫法失敗，自動折行不保證對齊                                        |
| 工作清單           | 清單項目開頭的 `[ ]`、`[x]` 改為 ☐、☑                                                                    |
| 表格               | `[table width=100% border=1 cellspacing=0 cellpadding=4]`，內含 `[tr]`、`[td]`；表頭加粗                 |
| 引用               | `[quote]` 保留多段、巢狀引用與內含清單                                                                   |
| 連結、圖片         | HTTP(S) 的 `[url=…]文字[/url]`、自閉合 `[img=…]`                                                         |
| 不可用網址         | 移除連結功能，保留標籤或圖片說明，顯示提醒                                                               |
| 行內程式碼         | 原文文字，移除反引號                                                                                     |
| 程式碼區塊         | 非空白行 `[div][font=Courier New]...[/font][/div]`；空白行單獨 `[div]&#160;[/div]`；Tab 預設四欄定位展開 |
| 原始 HTML          | 跳脫為文字，不執行                                                                                       |
| 公式               | 保留原文，不渲染；有辨識到公式時顯示提醒                                                                 |

文字中的 `&`、`<`、`>`、引號依輸出格式編碼；巴哈原始碼另將字面中括號編碼為 `&#91;`、`&#93;`。程式碼內容不再做 Markdown 轉換。

URL 先解析再限制為 HTTP(S)，拒絕控制字元、空白與內嵌帳密。URL 中的中括號、引號等可能結束屬性的字元改為百分比編碼；HTML 屬性另作 HTML escaping。

## 與貼文技能的刻意差異

這是忠實轉換器，不是文章編輯助手，因此不自動重寫摘要、補來源、新增章節編號、重排表格或把敘述轉成表格。Markdown 既有連結、圖片與字體樣式視為明確的格式意圖並保留。

舊版技能曾將各標記放在不同實體行；已更新技能與本工具一致，因巴哈編輯器實測會將這些分隔換行轉成額外 `<br>`。本工具移除自產標記間的分隔換行，保留 `[div]` 代表的每一行與空白行，避免程式碼出現雙倍行距。原始碼不包含外層 Markdown 圍欄。

## 尚未支援

AI 改寫、反向轉換、圖片上傳、公式排版、完整 GFM 擴充、相對網址解析與網站匯入。不擅自推測無法確認的語法支援。

## 證據與限制

見 [COMPATIBILITY.md](COMPATIBILITY.md) 的正式貼文證據。`[code]` 依使用者實測不使用；`[quote]` 已驗證可用。實體字面值的多層編碼仍被巴哈解碼，本工具保留本機原文並提醒。HTTP(S) allowlist、HTML 純文字處理及不渲染 LaTeX 是工具設計選擇，不是平台不支援的證明。

## 表格欄位對齊

只從 markdown-it 的表頭／資料格 token 保留 `left`、`center`、`right`，分別輸出 `[td align=…]` 及 HTML 內嵌 `text-align`。未指定不加屬性，不接受任意 CSS。純文字不模擬欄位對齊。HTML 捲動容器供預覽、複製與下載共用；原始碼只保留表格。每次有表格的轉換顯示一次手機溢出相容性提醒。

其他已測語法與裝置差異見 [文章預覽證據](docs/editor-preview-evidence.md)，不因平台支援而自動新增 Markdown 擴充。


## 程式碼上色

預設開啟，提供 Xcode、GitHub Light、VS2015 Dark，獨立於網站主題；設定不儲存。圍欄第一個語言識別字優先，無標示的圍欄／縮排區塊使用選單指定語言或自動偵測。text/txt/plaintext 保持單色，未知語言不猜測並提醒；行內程式碼不變。

上色區塊使用單格表格底色，非空白行仍為 div + Courier New，再以 color/bgcolor/b/i/u 呈現語法樣式。空白行不包字體，HTML 的內嵌配色同步用於複製和下載，純文字不含樣式。關閉上色恢復既有輸出。HTML 程式碼框可鍵盤水平捲動，不代表巴哈貼上後有相同捲動行為。

同步核心先展開 Tab，再以 highlight.js 對完整區塊上色，htmlparser2 只讀文字與 span/class，檢查還原文字相同後才產生受限標記；不直接執行上色器 HTML。每區塊僅計算一次供輸出共用。超過單區塊 20,000 或待處理合計 100,000 UTF-16 字元、解析／上色失敗者回退單色並提醒。自動偵測無結果也保留單色。

## 標題層級

井字與 Setext 標題均保留級別；前三級 HTML 使用 30／22／18px，深層為 1em，粗體、行高 1.5、margin 0 與長行折行皆內嵌。此為工具輸出設計，不保證與巴哈裝置樣式相同。沿用結構性標題前空白區塊，分隔線後不另加；多行 Setext 保留行內換行。巴哈不輸出未解析的 h5／h6；其四至六級統一為正文粗體，提醒每次轉換只出現一次。純文字內容與換行規則不變。
