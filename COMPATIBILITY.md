# 相容性與驗證紀錄

最新查證日期：2026-09-08。下方 2026-09-07 紀錄為歷史基線；最新結果以本節為準。

## 表格對齊與手機閱讀更新

本輪保留 Markdown 左／中／右對齊，HTML 每張表格提供有名稱、可聚焦的局部捲動容器，BBCode 仍為原有表格結構；每次轉換只出現一次表格手機限制提醒。61 項測試與正式建置通過，包含對齊、引用內表格、安全性、內容與剪貼簿／下載一致性。

本機 390×844 測試使用 24 欄長內容表格：頁面寬 390px，表格捲動區寬 308px、內容寬 745px，48 個儲存格完整保留；鍵盤 ArrowRight 可使 scrollLeft 增至 40。這是本機結果，不代表巴哈的樣式。

巴哈電腦／手機文章預覽已完成目視驗證；本輪 DOM 尺寸讀取逾時，精確量測未完成。詳見 [文章預覽證據與限制](docs/editor-preview-evidence.md)，與先前正式貼文證據分開解讀。

## 2026-09-08 優化版

### 正式貼文已驗證

[使用者發布的相容性測試](https://forum.gamer.com.tw/Co.php?bsn=60076&sn=109593747)：助手檢查正式 HTML 與元素尺寸，未代為發文。引用、多段與兩層引用、引用內清單、粗斜刪除線、表格 5 列各 3 欄、空白格、多行格與 URL 查詢參數通過。Courier New 保留且 ASCII 直線對齊。

- `[ol start=3]`、`[ol start=0]`、`[li value=10]` 成為字面標記，這些寫法失敗。本版使用文字編號與 NBSP 手動縮排，不保證懸掛對齊。
- `[div]&#160;[/div]` 保留一行高度；包在 font 內的 NBSP 空白行變成一般空格且高度為零，因此本版空白行不包字體。
- E1／E2／E3 的一層、兩層、三層實體編碼，正式發文後均變成 `& | [ | ] | < | >`。本版顯示限制提醒，不破壞本機原文或宣稱已修復。
- 先前測試文章的外部 Octocat 圖片經延遲載入後可顯示；不能據此保證所有圖片來源或富文字貼上。

### 編輯器已驗證：本次整合輸出

macOS 的 Codex 內建瀏覽器，已登入巴哈完整空白回覆編輯器。本機新版範例原始碼填入後切回排版，DOM 確認：2 層 blockquote、5 個 Courier New 字體元素、5 個空白區塊皆高 25.5px，文字編號 3／4 及四格 NBSP 保留。表格和連結仍在。

本次編輯器切換後 `&amp;`、`&#91;`、`&#x20;` 的拼寫仍保留，與先前正式發文結果不同；尚不能確定發生變化的階段。測試結束已清空並關閉編輯器，沒有發文、修改既有文章或寄信。

### 使用者回報

`[code]` 確實不支援。本次正式測試中的字面 `[code]` 僅驗證 escaping，不當作另一次真正 code tag 測試。

### 本機自動化與瀏覽器

59 項 Vitest 測試及 TypeScript／正式建置通過，新增引用 DOM、清單縮排、段落間距、code 空白行、實體提醒正反例，以及三種內容跨預覽／雙剪貼簿／下載的一致性驗證。

本機瀏覽器載入新版範例確認巢狀引用、等寬樣式與空白行高度約 25.9px；兩種複製回報成功，HTML／TXT 下載按鈕已操作，檔案內容由自動化測試比對。桌面 1280px 與手機 390×844 的頁面寬度均等於視窗寬度；手機輸入與輸出採上下排列。深淺主題切換可操作，手機瀏覽器未回報頁面錯誤。

### 尚未驗證與工具設計

新版整合輸出尚未正式發文；一般人工富文字貼到巴哈後的樣式保留尚未確認，自動化 clipboard payload 測試不等於作業系統人工貼上。其他編輯器、手機 App、所有圖片來源與其他起始編號寫法未驗證。

HTTP(S) allowlist、不執行 HTML、不渲染 LaTeX 是工具設計選擇；未收錄的標籤不等於平台不支援。

## 2026-09-07 歷史基線

這份紀錄區分「轉換器通過測試」、「當次編輯器可呈現」與「正式發文可用」，不將網站預覽當成巴哈官方結果。

## 巴哈編輯器實測

環境：macOS、Microsoft Edge、當次已登入的巴哈完整回覆編輯器。只測試空白編輯區，結束前清空所有測試內容並關閉頁面，沒有發文或寄送站內信。

| 項目           | 結果                                                                                                      |
| -------------- | --------------------------------------------------------------------------------------------------------- |
| 原始碼入口     | 點入空白編輯區後，工具列出現可操作的「原始碼」按鈕                                                        |
| 標題、粗體     | 內建範例轉回排版後，8 個粗體元素存在                                                                      |
| 表格           | 1 張表格、4 列、3 欄，共 12 個儲存格                                                                      |
| 巢狀清單       | `ul ul` 層級保留，工作清單符號保留                                                                        |
| 網頁連結       | `https://www.gamer.com.tw/` 連結正確保留                                                                  |
| 程式碼         | 字面 `[code]`、`<div>` 顯示為文字，四格縮排保留                                                           |
| 空白行         | 範例中的一個空白區塊保留                                                                                  |
| 多餘換行       | 修正前標記間換行被轉成 `<br>`；緊湊輸出複驗後，頂層額外 `<br>` 數量為 0                                   |
| 富文字剪貼簿   | 瀏覽器回報寫入成功，測試也確認 HTML/plain payload；自動化貼上通道未保留完整樣式，無法判定一般人工貼上結果 |
| 外部圖片       | 未在巴哈實測；可能需重新上傳                                                                              |
| 正式發文後呈現 | 未測試，沒有送出任何文章                                                                                  |

站務在 [2026-06-04 回覆](https://forum.gamer.com.tw/Co.php?bsn=60404&sn=105975) 中曾表示關閉原始碼入口。本次觀察只能證明當前瀏覽器仍有此入口，無法區分是否為恢復開放、帳號差異、前端版本或瀏覽器擴充影響。因此網站文案採「入口依編輯器而異」，不宣稱所有人都已恢復或都已關閉。

## 自動化測試

`npm test` 執行 Vitest；本次共 47 項測試通過，TypeScript 檢查與正式建置通過。涵蓋：

- 標題、段落、跨行粗體、巢狀樣式、CRLF 與字元編碼。
- 巢狀清單、非 1 起始編號、工作清單、表格欄列與跳脫管線符號。
- 反引號、波浪號及縮排程式碼、未閉合圍欄、空白行、Tab 定位、反斜線及字面標記。
- HTTP(S)、無效 URL、本機檔案、含帳密 URL、JavaScript/data URL、圖片與屬性跳脫。
- 原始 HTML 不進入可執行 DOM，公式保持原文。
- 真正掛載網站 HTML 與主程式的 DOM 測試：即時轉換、中文字元組字、分頁鍵盤操作、快捷鍵、範例、清空、雙剪貼簿 payload、拒絕時的替代流程、下載檔案內容與只儲存主題。

剪貼簿拒絕及 API 行為在 DOM 測試使用替身，不能取代所有真實瀏覽器權限與作業系統剪貼簿實測。

## 瀏覽器驗證

- 桌面深／淺色頁面可載入，無應用程式 console error／warning。
- 導覽、範例、清空、即時輸入、預覽／原始碼切換正常。
- 「複製原始碼」可從剪貼簿讀回正確標記。
- 兩種下載已從瀏覽器下載到本機，實際檔案比對確認：TXT 等於轉換原始碼，HTML 包含完全相同的排版內容。
- 390 × 844 手機尺寸採上下雙欄，頁面寬度等於視窗寬度，無整頁橫向溢出。
- 程式碼的 Tab 與空白行在本機預覽呈現正確。
- 完整富文字人工貼上、Safari、Firefox、iOS、Android 與正式發文結果仍未測試。

## 正式部署

GitHub Actions 的測試、建置與 Pages 部署皆通過。初次部署沿用帳號既有 Pages 網域 `blog.jakeuj.com` 並啟用 HTTPS。首頁、JS、CSS、favicon 皆回傳 200；原始 GitHub Pages 網址可正確轉址。正式站的範例轉換與原始碼剪貼簿內容已再次確認，沒有應用程式 console error／warning。

## 獨立網域

正式網址改為 [bahamut-markdown-converter.jakeuj.com](https://bahamut-markdown-converter.jakeuj.com/)。Cloudflare DNS-only CNAME 指向 `jakeuj.github.io`，GitHub Pages 設定綁定相同網域；Vite 資源路徑改為相對路徑，支援根目錄部署。

## 來源

- 作者提供的巴哈貼文技能規則：[RULES.md](RULES.md)。
- [站務回覆](https://forum.gamer.com.tw/Co.php?bsn=60404&sn=105975)。
- [阿冷 Arlen 工具](https://www.arlenfuture.com/tools/markdown-to-bbcode/)：以引用替代 `[code]` 的經驗，本專案採逐行區塊。
- [參考網站](https://discord-markdown-converter.gh.miniasp.com/)：操作及資訊架構參考。
